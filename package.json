{
    "name": "FullstackTestingExam",
    "jest": {
        "testMatch": [
            "**/*.steps.js"
        ]
    },
    "scripts": {
        "jest": "./node_modules/.bin/jest",
        "test:jest": "npm run jest -- --config ./package.json"
    },
    "devDependencies": {
        "fs": "0.0.1-security"
    },
    "dependencies": {
        "archiver": "^5.0.0",
        "jest": "^26.2.2",
        "jest-cli": "^26.2.2",
        "jest-cucumber": "^3.0.0",
        "supertest": "^4.0.2"
    },
    "cypress-cucumber-preprocessor": {
        "nonGlobalStepDefinitions": false
    }
}
